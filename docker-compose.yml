version: "3.9"

services:

  ai-payment-assistant:
    build: .

    container_name: ai-payment-assistant

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - .:/app

    restart: unless-stopped